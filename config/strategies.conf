# Strategy parameters (sourced by simulate.sh).
BUY_PCT=1.0          # buy when up >= this % in lookback window
LOOKBACK_MIN=5       # lookback window in minutes
HOLD_MIN=15          # max hold time in minutes
STOP_PCT=2.0         # stop-loss in %
CASH=10000           # starting cash per ticker
SHARES=10            # shares per trade
